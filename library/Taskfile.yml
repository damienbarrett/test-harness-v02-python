version: "3"

vars:
  TOOL_PATH:
    sh: printf '%s' "$HOME/.local/bin:$HOME/.cargo/bin:$HOME/go/bin:$PATH"
  HARNESS_DIR:
    sh: printf '%s' "${HARNESS_DIR:-{{.TASKFILE_DIR}}/.harness}"
  HARNESS_OUTPUT_DIR:
    sh: printf '%s' "${HARNESS_OUTPUT_DIR:-${HARNESS_DIR:-{{.TASKFILE_DIR}}/.harness}/outputs}"
  HARNESS_CACHE_DIR:
    sh: printf '%s' "${HARNESS_CACHE_DIR:-${HARNESS_DIR:-{{.TASKFILE_DIR}}/.harness}/cache}"
  UV_CACHE_DIR:
    sh: printf '%s' "${UV_CACHE_DIR:-${HARNESS_CACHE_DIR:-${HARNESS_DIR:-{{.TASKFILE_DIR}}/.harness}/cache}/python/library/uv}"

env:
  PATH: "{{.TOOL_PATH}}"
  HARNESS_DIR: "{{.HARNESS_DIR}}"
  HARNESS_OUTPUT_DIR: "{{.HARNESS_OUTPUT_DIR}}"
  HARNESS_CACHE_DIR: "{{.HARNESS_CACHE_DIR}}"
  UV_CACHE_DIR: "{{.UV_CACHE_DIR}}"

tasks:
  default:
    cmds:
      - task --list
    silent: true

  setup:
    desc: Install dependencies
    cmds:
      - uv sync --locked --extra test

  test:
    desc: Run tests
    cmds:
      - uv run --locked --extra test pytest tests/ -v

  coverage:
    desc: Run tests with coverage
    cmds:
      - uv run --locked --extra test pytest tests/ --cov --cov-report=term-missing

  clean:
    desc: Remove generated outputs while preserving dependency state
    cmds:
      - rm -rf __pycache__ .pytest_cache .coverage htmlcov output "$HARNESS_OUTPUT_DIR/python/library" tests/__pycache__ src/tasks/__pycache__
      - find . -name '*.egg-info' -exec rm -rf {} + 2>/dev/null || true

  purge:
    desc: Remove generated outputs and setup artifacts
    cmds:
      - task: clean
      - rm -rf .venv "$UV_CACHE_DIR" "{{.TASKFILE_DIR}}/.harness"
